{{#progress}}
    <div class="progress mb-0">
        <div class="progress-bar" role="progressbar" aria-valuenow="{{percent}}" aria-valuemin="0"
             aria-valuemax="100" style="min-width: 2em; width: {{percent}}%;">
            {{percent}}%
        </div>
    </div>
{{/progress}}
